apply plugin: 'java'

jar.baseName = 'JaWS'

jar {
   manifest {
       attributes 'Main-Class': 'jaws.Main'
   }
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile group: 'com.google.code.gson', name: 'gson', version: '2.6.2'
}
